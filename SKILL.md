---
name: aliyun-mp-writer
description: 将内容素材整理输出为适合在阿里云官方微信公众号发布的文章。支持 Word(.docx)、PDF(.pdf)、Markdown(.md)、纯文本及在线链接等素材形式。Use when the user asks to write, draft, or format articles for 阿里云微信公众号, 阿里云官微, AliWeChat, or when providing content素材 (documents or links) to be transformed into 阿里云-style WeChat official account articles.
---

# 阿里云官微文章写作 Skill

将技术素材、产品文档、新闻稿等内容转化为符合阿里云官方微信公众号风格的专业文章。

## 工作流程

```
Task Progress:
- [ ] Step 1: 接收并解析素材
- [ ] Step 2: 提炼核心信息（业务背景、痛点、方案、价值）
- [ ] Step 3: 按照阿里云官微结构组织文章
- [ ] Step 4: 按 style-reference.md 中的风格要求润色
- [ ] Step 5: 按用户指定格式输出文件
```

## Step 1: 接收并解析素材

支持以下输入形式，需选择合适的工具读取和解析：

### 1.1 文档文件

根据文件格式选择对应的读取方式：

| 格式 | 读取方式 | 说明 |
|------|----------|------|
| Word (.docx) | 使用 `docx` skill | 调用 docx skill 提取文本和表格内容 |
| PDF (.pdf) | 使用 `pdf` skill | 调用 pdf skill 提取文本、表格和结构化内容 |
| Markdown (.md) | 使用 Read 工具直接读取 | 纯文本格式，直接读取即可 |
| 纯文本 (.txt) | 使用 Read 工具直接读取 | 直接读取文件内容 |

### 1.2 在线链接

| 类型 | 读取方式 | 说明 |
|------|----------|------|
| 普通网页/文章 | 使用 WebFetch 工具 | 抓取页面内容并转换为可读文本 |
| 需要交互的网页 | 使用 builtin_browser MCP | 通过浏览器自动化 navigate → read_page/get_page_text 获取内容 |

### 1.3 直接文本

用户直接在对话中粘贴或输入的文本素材，直接处理即可。

### 素材提取目标

无论素材形式如何，最终需提取以下核心信息：
1. 涉及的产品/技术是什么
2. 解决的业务问题或行业挑战
3. 核心技术能力或方案
4. 效果数据或客户案例（如有）

## Step 2: 内容组织

按照以下结构组织内容（可根据素材实际情况调整）：

### 标准结构

1. **标题**：宣告式 + 核心价值，如「阿里云 XXX 正式发布：YYY，助力 ZZZ」
2. **开篇**：宣告式开头 + 产品定位 + 核心价值一句话
3. **业务背景**：行业趋势、技术浪潮、时代背景
4. **业务痛点**：当前面临的挑战（2-4 个核心痛点）
5. **方案详解**：分模块技术解读，每模块采用「挑战 → 方案 → 效果」三段式
6. **客户/案例**：客户证言、合作案例、效果数据
7. **未来展望**：「未来，阿里云将继续…」式结尾 + 行动号召

### 结构变体

根据素材类型可选择不同侧重：

| 素材类型 | 侧重章节 | 简化章节 |
|----------|----------|----------|
| 产品发布 | 方案详解 + 技术能力 | 业务背景（简略） |
| 客户案例 | 痛点 + 方案 + 效果数据 | 技术原理（简略） |
| 行业解读 | 业务背景 + 趋势分析 | 方案详解（简略） |
| 技术深度 | 方案详解（架构级拆解） | 客户案例（可选） |

## Step 3: 撰写要求

**必须读取并遵循 [style-reference.md](style-reference.md) 中的风格规范。**

关键要求摘要：

- **语言**：中文为主，专业术语和產品名保留英文原称（Serverless、Agent、TCO 等）
- **句式**：长句为主，典型「技术特性+业务价值」双段式句型
- **结构**：「总-分-总」，章节采用「挑战→方案→效果」三段式
- **数据**：技术 claim 必须有具体数字支撑
- **结尾**：几乎每篇都使用「未来，阿里云将继续…」展望式结尾
- **高频词**：企业级、生产环境、极致、一站式、全链路、开箱即用、规模化落地

## Step 4: 识别输出格式并按需输出

### 4.1 识别用户指令

检查用户的输出格式要求：

- 用户明确要求 **Word** → 使用 `docx` skill 生成 .docx 文件
- 用户明确要求 **PDF** → 使用 `pdf` skill 生成 .pdf 文件
- 用户明确要求 **Markdown** 或未指定格式 → 默认输出 .md 文件

文件名格式：`aliyun-mp-{主题关键词}-{日期}.{扩展名}`

### 4.2 排版要求

无论输出何种格式，文章必须保持简洁清晰的排版：

- 使用层级标题（H1/H2/H3）组织内容结构
- 章节之间空一行，保持视觉呼吸感
- 关键数据和结论使用**加粗**突出
- 功能列举使用编号或无序列表
- 客户证言使用引用块（`>`）格式
- 技术对比使用表格呈现（如有多维度对比数据）
- 段落长度控制在 100-300 字，避免大段密集文字

### 4.3 保存位置

将文件保存到用户选定的工作目录，并生成 file:// 链接供用户查看。

## 文章模板

详细模板参见 [article-template.md](article-template.md)，包含：
- 完整文章骨架
- 各章节写作模板
- 常用句式与段落模板
- 标题拟定指南

## 注意事项

- 保持阿里云特有的「工程师气质」——严谨、克制、以数据说话
- 情感表达通过技术成就和客户背书间接传递，不直接抒情
- 技术描写采用「架构分层法」，按计算层、存储层等逐层展开
- 每个技术能力都要回答「如何帮助 XX 解决 YY 问题，实现 ZZ 价值」
- 避免过度营销化语言，保持科技大厂的理性务实风格
